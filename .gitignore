# ── R / RStudio ──────────────────────────────────────────────
.RData
.RDataTmp*
.Rhistory
.Rproj.user/

# ── Large model & data outputs (regenerable from scripts) ──
*.rds
*.RData

# ── Generated results (regenerable from scripts) ───────────
Q*_*.csv
loo_cv_predictions*.csv

# ── Generated HTML (render from .md / .Rmd) ────────────────
*.html

# ── Word documents ─────────────────────────────────────────
*.docx

# ── Excluded files (superseded scripts, old figures, etc.) ─
_exclude/

# ── Archive (superseded files, not for version control) ────
_archive/

# ── Cache (WorldClim rasters etc., regenerable) ────────────
cache/

# ── Editor metadata ────────────────────────────────────────
.positai/

# ── OneDrive conflict artifacts ────────────────────────────
*conflicted copy*
~$*

# ── Misc ───────────────────────────────────────────────────
Rplots.pdf

# ── Figures: track PNGs (README) and PDFs (publication) ────
# All figures live in figures/; nothing at root level
